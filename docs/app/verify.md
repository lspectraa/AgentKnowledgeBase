# Verify

When UI or runtime behavior changed, check the running app — do not finish on code-read alone.

Verify once at the end of the change, not after every edit. Skip when the change has no runtime effect (docs, CI config).

Stop anything this session started when finished.
