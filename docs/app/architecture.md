# Architecture

Single systems guide for this app. Keep new subsystem detail in this file. Do not add `docs/app/systems/*.md`.

When indexing, record here: purpose, main flows, layout, important modules, contracts, and invariants. Prefer evidence (paths, symbols) over guesses.

## Systems

_Fill on first index. One heading per subsystem._
