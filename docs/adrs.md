# ADRs

Accepted decisions live in `docs/adrs/` as `NNN-short-slug.md`. Create them with [[living-adr]].

Each ADR needs a matching comment above the code it governs, pointing at that file. List those sites under **Code anchors** in the ADR.
