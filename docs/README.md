# Agent Knowledge Base

Living Markdown under `docs/`. Canonical instructions: repo-root `AGENTS.md`. Canonical skills: `skills/`.

Optional adapters: `CLAUDE.md`, `.github/copilot-instructions.md`, `.cursor/rules/`, `.cursor/hooks.json`.

Notes use `[[wikilinks]]` only when the next file must be opened. No Related sections.

Install: `docs/prompts/integrate-into-repo.md`.
